import 'package:flutter/material.dart';

class AlzheimersScreen extends StatelessWidget {
  const AlzheimersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Alzheimer's Screen"),
      ),
    );
  }
}
