import 'package:flutter/material.dart';

class BrainTumorScreen extends StatelessWidget {
  const BrainTumorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Brain Tumor Screen"),
      ),
    );
  }
}
